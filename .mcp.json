{
  "mcpServers": {
    "researcher-mcp": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}/core",
        "--extra",
        "mcp",
        "researcher-mcp"
      ],
      "env": {}
    }
  }
}
