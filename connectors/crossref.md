# Crossref Connector

CrossRef REST API integration (https://api.crossref.org/). No key required but set mailto for polite pool. Supports: DOI resolution, metadata lookup, reference validation, citation counts.
