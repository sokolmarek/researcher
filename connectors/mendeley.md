# Mendeley Connector

Mendeley reference manager integration. Uses Mendeley REST API. Requires MENDELEY_TOKEN (OAuth2). Supports: library access, document retrieval, annotation sync, group libraries.
